diff --git a/viewer/views/mixins.pug b/viewer/views/mixins.pug
index ab39c69d..89a577e7 100644
--- a/viewer/views/mixins.pug
+++ b/viewer/views/mixins.pug
@@ -54,6 +54,8 @@ mixin arrayList(container, field, title, expr, extra, max, session)
         if (container[field].length > max)
           |<a class="cursor-pointer show-more-items" @click="showFewerItems($event)">less...</a></span>
           a.cursor-pointer.show-more-items(@click="showMoreItems($event)") more...
+      else
+        span &lt;empty&gt;
       if (extra)
         |!{extra}
       div(style="display:inline;") &nbsp;
