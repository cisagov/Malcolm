name: freq-build-and-push-ghcr

on:
  push:
    branches:
      - main
      - staging
      - pipeline-merge
    paths:
      - 'freq-server/**'
      - 'Dockerfiles/freq.Dockerfile'
      - 'container-health-scripts/freq.sh'
      - 'shared/bin/docker-uid-gid-setup.sh'
      - 'shared/bin/service_check_passthrough.sh'
      - '.trigger_workflow_build'
  workflow_dispatch:
  repository_dispatch:

jobs:
  docker:
    uses: ./.github/workflows/docker-build-push-scan.yml
    with:
      service: freq
      dockerfile: ./Dockerfiles/freq.Dockerfile
