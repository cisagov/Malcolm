output {
  opensearch {
    id => "output_opensearch_malcolm"
    hosts => "${OPENSEARCH_HOST_AND_PORT:opensearch:9200}"
    manage_template => false
    index => "%{[@metadata][malcolm_opensearch_index_prefix]}-%{+YYMMdd}"
    document_id => "%{+YYMMdd}-%{[event][hash]}"
  }
}
